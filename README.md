# Jazz Solo Practice

Generate random jazz + basic step combinations for dance practice.

## Use

Tap **Generate** (or press Space/Enter) to get new combinations. Open settings to configure step counts and starting mode.

## Dev

Open `index.html` in a browser. No build step.

Edit `steps.json` to add/change steps.
